/* Skill Entity */
var Skill = function (skillId, skillName, skillDescription) {

    this.skillId = skillId;
    this.skillName = skillName;
    this.skillDescription = skillDescription;
}

module.exports = Skill;
