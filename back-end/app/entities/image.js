/* Image Entity */
var Image = function (imageId, eventId, uploadedDate, imageLink) {

    this.imageId = imageId;
    this.eventId = eventId;
    this.uploadedDate = uploadedDate;
    this.imageLink = imageLink;
}

module.exports = Image;
